import { NavAdmin } from "@/components/common/NavBar";
import Box from "@mui/material/Box";

const index = () => {
  return (
    <Box
      sx={{
        width: "100%",
        height: "100vh",
      }}
    >
      <NavAdmin />
    </Box>
  );
};

export default index;
